#include <stdio.h>

// this function should be able to return both the multiplied and the added version of the result
int our_funtion(int a, int b)
{

    int result = a + b;
    return result;
}

int main()
{

    int answer = our_funtion(3, 5);

    return 0;
}
